services:
  - type: web
    name: fastapi-backend
    runtime: python
    buildCommand: "pip install -r back/requirements.txt"
    startCommand: "python -m uvicorn back.main:app --host 0.0.0.0 --port 10000"
    plan: free